import React from "react";

const Header = (props) => {
  return (
    <header className="App-header">
      <h1>{props.text} React Hooks : Movie App</h1>
    </header>
  );
};

export default Header;